    <% layout("layouts/boilerplate") %>
    <h1>Edit Campground</h1>
    <!-- Form for editing a campground -->
    <form action="/campgrounds/<%= campground._id%>?_method=PUT" method="POST">
        <!-- Field for editing the title of the campground -->    
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="campground[title]" value="<%= campground.title%>">
        </div>
        <!-- Field for editing the location of the campground -->
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="campground[location]" value="<%= campground.location%>">
        </div>
        <!-- Button for submitting the form -->
        <button>Update Campground</button>
    </form>
    <!-- Link for going back to the campground details page -->
    <a href="/campgrounds/<%= campground._id%>">Back to Campground</a>
